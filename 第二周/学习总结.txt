编写子域名访问计数时出现2个问题
1.putOneLeverDomainVisitTimes2Hash函数前面计算字符串哈希值时没有将指针缓存到本地变量，导致计算完哈希后字符串指针已经指向最后，后面的字符串比较就一直是空了  --对于函数中的指针变量，要关注操作指针偏移的逻辑，如果后续还要使用原始的指针，需要缓存指针变量
2.最后遍历哈希表输出时，只输出了哈希表链表的第一个元素，忘记把冲突的链表上的数据都输出了  --哈希只是尽可能做到均匀分开，但是在映射不是一一对应的场景下数据冲突是不可避免的，一定要考虑冲突数据的处理